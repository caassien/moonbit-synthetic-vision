# Examples

Run the smoke example from the repository root:

```bash
moon run --target wasm-gc examples/smoke
```

It prints a compact calibration board and a seeded noisy target scene. The
example is intentionally text-only so it works in CI and terminals without
extra image viewers.
